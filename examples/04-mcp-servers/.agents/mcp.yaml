servers:
  postgres:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-postgres"
      - "postgresql://localhost:5432/issues"
    env:
      PGCONNECT_TIMEOUT: "5"

  linear:
    url: https://mcp.linear.app/sse
